---
'@composio/core': patch
---

Add experimental Smart MCP Tool Exposure for Tool Router sessions with adaptive discovery modes (`shadow`, `soft`, `strict`), active-set preservation, mandatory tool/toolkit retention, confidence fallback, and a refresh meta-tool (`COMPOSIO_REFRESH_TOOLS`).
